---
services:
  elasticsearch:
    image: '${SEARCH_IMAGE:-opensearchproject/opensearch:2}'
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      - DISABLE_INSTALL_DEMO_CONFIG=true
      - DISABLE_SECURITY_PLUGIN=true
    volumes:
      - ./data/opensearch:/usr/share/opensearch/data
